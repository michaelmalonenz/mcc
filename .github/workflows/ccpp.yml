name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: setup
      run: |
        mkdir build
        cd build
        cmake ../
        make
        make test
        ctest -D MemCheck
